import 'package:flutter/material.dart';

class AppColors {
  // 🌿 Primary Brand Colors
  static const Color white = Color(0xFFFFFFFF);
  static const Color primary = Color(0xFF5E8C61);          // Vibrant forest green
  static const Color primaryLight = Color(0xFFA7D8A8);     // Soft mint green
  static const Color primaryLighter = Color(0xFFCFF5D2);   // Light pastel green
  static const Color primaryDark = Color(0xFF3E6145);      // Deep earthy green

  // 🪵 Secondary Colors
  static const Color secondary = Color(0xFF98A88C);        // Balanced sage green
  static const Color secondaryDark = Color(0xFF4B5E47);    // Dark desaturated green

  // 🎨 Backgrounds & Surface
  static const Color background = Color(0xFFF9F9F9);        // Soft off-white
  static const Color surface = Color(0xFFE8F5E9);           // Light green surface

  // ✨ Accents & Highlights
  static const Color accentBright = Color(0xFF6FCF97);      // Fresh bright green (FAB)
  static const Color accentLight = Color(0xFFD1F5DC);       // Light mint hover
  static const Color accentHover = Color(0xFFB5F0C4);       // Hover mint
  static const Color accentShadow = Color(0x336FCF97);      // Shadow green with opacity

  // 🎯 Iconography
  static const Color iconActive = Color(0xFF2F5233);        // Strong active icon green
  static const Color iconInactive = Color(0xFF9BA3A4);      // Muted grey for inactive icons

  // 🛑 States (optional)
  static const Color error = Color(0xFFE57373);             // Soft red
  static const Color warning = Color(0xFFFFB74D);           // Amber orange
  static const Color success = Color(0xFF81C784);           // Fresh success green
  static const Color info = Color(0xFF64B5F6);              // Calm blue

  // 🖋️ Text contrast colors
  static const Color textPrimary = Color(0xFF2E2E2E);       // Rich black
  static const Color textSecondary = Color(0xFF616161);     // Medium gray

  static const Color disabledBackground = Color(0xFFF5F5F5);
  static const Color textDisabled = Color(0xFFBDBDBD);
}
